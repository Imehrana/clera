/**
 * HeroTransition — soft gradient fade at the bottom of the Hero.
 * Overlaps the last 120 px of the hero with a transparent→cream blend.
 * No shapes, no waves, no JS — pure CSS.
 */
export default function HeroTransition() {
  return <div className="hero-fade" aria-hidden="true" />;
}
